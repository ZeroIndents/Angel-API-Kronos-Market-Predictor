# ---------------------------------------------------------------------------
# Kronos View - live chart terminal dependencies
# ---------------------------------------------------------------------------
# Windows:  py -m venv .venv
#           .venv\Scripts\python.exe -m pip install -r requirements.txt
#           .venv\Scripts\python.exe server.py
#
# The NVIDIA Kronos model repo (github.com/shiyu-coder/Kronos) is imported at
# runtime - clone it next to this folder (or set KRONOS_REPO_DIR).
# ---------------------------------------------------------------------------

# --- Server -----------------------------------------------------------------
fastapi>=0.115          # REST API framework
uvicorn>=0.34           # ASGI server
pandas>=2.2.2
numpy>=1.26
pydantic>=2.0

# --- Angel One SmartAPI (8_smartapi_auth.py, 9_smartapi_fetch.py, 10_smartapi_live.py)
smartapi-python>=1.4.8  # official Angel One SDK
pyotp>=2.10             # TOTP generation for the daily 2FA login
logzero>=1.7            # smartapi-python dependency
websocket-client>=1.8   # SmartWebSocketV2 dependency

# --- Kronos inference (2_kronos_inference.py) -------------------------------
# CPU-only build: runs on ANY machine — no GPU required.
# The Kronos models are tiny (4.1M / 24.7M params) and run on CPU in
# seconds. Install the default (CPU) PyTorch wheel:
torch>=2.2
huggingface_hub==0.33.1 # pinned by the Kronos repo
einops==0.8.1
safetensors>=0.6.2
tqdm>=4.67.1
matplotlib>=3.9.0       # Kronos repo dependency
